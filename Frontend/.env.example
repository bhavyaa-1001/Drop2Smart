# Google Maps API Configuration (Optional - for full map features)
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Make sure to enable the following APIs:
# - Maps JavaScript API
# - Places API
# - Geocoding API
# Leave blank to use IP-based location detection only

# Location Services Configuration
# The app uses multiple location detection methods:
# 1. Browser Geolocation API (requires HTTPS)
# 2. IP-based location (ipapi.co, ipinfo.io, ip-api.com)
# 3. Google Maps Geocoding (if API key provided)
# 4. City database fallback

# Other environment variables
REACT_APP_API_BASE_URL=http://localhost:3001
REACT_APP_ENVIRONMENT=development
